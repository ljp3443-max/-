# 화면(HTML)을 창에 띄우고 파이썬과 이어 주는 도구.
# 윈도우에서는 pythonnet 을 함께 끌어옵니다(Edge WebView2 백엔드용).
pywebview>=5.0

# exe 로 묶을 때만 필요합니다. 개발 중에는 없어도 됩니다.
pyinstaller>=6.0

# 엔진(engine.py)은 표준 라이브러리만 씁니다. 여기에 더 넣을 것이 없습니다.
